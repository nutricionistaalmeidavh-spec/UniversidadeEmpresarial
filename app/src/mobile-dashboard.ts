const esc=(v:unknown)=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c] as string));const money=(v:unknown)=>new Intl.NumberFormat('pt-BR',{style:'currency',currency:'BRL'}).format(Number(v||0)/100);const card=(title:string,rows:Array<[string,string]>)=>`<section class="card management-module"><strong>${esc(title)}</strong>${rows.map(([a,b])=>`<div class="management-line"><span>${esc(a)}</span><b>${esc(b)}</b></div>`).join('')}</section>`;export function managementHtml(summary:any,access:any){const m=summary?.modules||{},parts:string[]=[];if(access?.modules?.includes('finance')&&m.finance)parts.push(card('Financeiro',[['A pagar',money(m.finance.payablePending)],['A receber',money(m.finance.receivablePending)],['Vencidos',String(m.finance.overdue||0)]]));if(access?.modules?.includes('dre')&&m.dre)parts.push(card('DRE',[['Receitas realizadas',money(m.dre.revenue)],['Despesas realizadas',money(m.dre.expense)],['Resultado',money(m.dre.result)]]));if(access?.modules?.includes('rh')&&m.rh)parts.push(card('RH',[['Funcionários ativos',String(m.rh.activeEmployees||0)],['Folhas abertas',String(m.rh.openPayrolls||0)],['Pagamentos pendentes',String(m.rh.pendingPayments||0)]]));if(access?.modules?.includes('contracts')&&m.contracts)parts.push(card('Contratos',[['Ativos',String(m.contracts.active||0)],['Valor contratado',money(m.contracts.totalValue)],['Aditivos',String(m.contracts.addenda||0)]]));if(access?.modules?.includes('procurement')&&m.procurement)parts.push(card('Compras',[['Solicitações abertas',String(m.procurement.requestsOpen||0)],['Pedidos abertos',String(m.procurement.ordersOpen||0)]]));if(access?.modules?.includes('measurements')&&m.measurements)parts.push(card('Medições',[['Total',String(m.measurements.total||0)],['Em aberto',String(m.measurements.open||0)],['Valor líquido',money(m.measurements.netValue)]]));if(access?.modules?.includes('documents')&&m.documents)parts.push(card('Documentos',[['Documentos',String(m.documents.total||0)],['Vencem em 30 dias',String(m.documents.expiring30d||0)]]));return parts.join('')||'<div class="empty card">Nenhum resumo administrativo foi publicado pelo Desktop para os módulos desta conta.</div>'}export function renderManagementDashboard(root:HTMLElement,summary:any,access:any,title='Gestão'){root.innerHTML=`<section class="hero management-hero"><small>Consulta executiva</small><div class="date">${esc(title)}</div><p>Dados somente para visualização. Alterações administrativas continuam no FLUXODRE Desktop.</p></section><section class="section"><div class="section-head"><div><h2>Indicadores</h2><p>${summary?.generatedAt?`Atualizados pelo Desktop em ${esc(new Date(summary.generatedAt).toLocaleString('pt-BR'))}`:'Aguardando publicação do Desktop.'}</p></div></div><div class="management-grid">${managementHtml(summary,access)}</div></section>`}
