import { db, error, json, requireAuth, withScopes } from '@appdeploy/sdk';
import { evaluateObjectiveConsolidation } from './unit-policy';
type UnitProgress={status:'practice'|'consolidated'|'review'|'pending_review';attempts:number;correct:number;errors?:number;hints?:number;durationSec?:number;questionStats?:Array<{attempts:number;errors:number}>;reviewStage?:number;reviewCount?:number;intervalDays?:number;completedAt?:string;nextReviewAt?:string};type EduRole='superadmin'|'admin'|'rh'|'gestor'|'colaborador';type EduParticipant={name:string;phone:string;email?:string;jobRole:string;role?:EduRole;status:'active'|'inactive';passwordHash:string;passwordSalt:string;mustChangePassword:boolean;level?:string;skillLevels?:Record<string,string>;progress?:number;completedModules?:string[];completedUnits?:string[];readingScore?:number;writingScore?:number;numeracyScore?:number;diagnosticCompletedAt?:string;unitProgress?:Record<string,UnitProgress>;skillScores?:Record<string,number>;createdAt:string;updatedAt:string;pendingReviews?:ReviewRecord[]};type EduSession={participantId:string;expiresAt:string;createdAt:string};type DiagnosticRecord={participantId:string;context:Record<string,string>;answers:Record<string,string>;readingScore:number;writingScore:number;numeracyScore:number;overallScore:number;level:string;createdAt:string};type ReviewRecord={id:string;participantId:string;unit:string;skill:string;level:string;question:string;response:string;submittedAt:string;attempts:number;correct:number;errors:number;questionStats?:Array<{attempts:number;errors:number}>};const EDU_ROLES:EduRole[]=['superadmin','admin','rh','gestor','colaborador'],ADMIN_ROLES:EduRole[]=['superadmin','admin','rh'],MODULES=['leitura','compreensao','escrita','adicao-subtracao','multiplicacao','divisao','porcentagem','medidas','seguranca','direitos','saude','tecnologia'];const enc=new TextEncoder(),now=()=>new Date().toISOString(),rec=(v:unknown):Record<string,unknown>=>v&&typeof v==='object'&&!Array.isArray(v)?v as Record<string,unknown>:{};const strMap=(v:unknown)=>Object.fromEntries(Object.entries(rec(v)).map(([k,x])=>[k,String(x??'')]));const keyHash=(v:string)=>{let h=2166136261;for(let i=0;i<v.length;i++){h^=v.charCodeAt(i);h=Math.imul(h,16777619)}return(h>>>0).toString(16)};const phoneTable=(p:string)=>'edu_phone_'+keyHash(p),emailTable=(e:string)=>'edu_email_'+keyHash(e),centralGrantTable=(e:string)=>'edu_central_grant_'+keyHash(e),sessionTable=(t:string)=>'edu_session_'+keyHash(t),diagnosticTable=(id:string)=>'edu_diagnostic_'+id.replace(/[^a-zA-Z0-9_-]/g,'_');function phone(v:string){let d=v.replace(/\D/g,'');if(d.length===10||d.length===11)d='55'+d;return d.length>=12&&d.length<=13?d:''}function email(v:string){const e=v.trim().toLowerCase();return /^\S+@\S+\.\S+$/.test(e)?e:''}async function hash(p:string,s:string){const b=await crypto.subtle.importKey('raw',enc.encode(p),'PBKDF2',false,['deriveBits']),bits=await crypto.subtle.deriveBits({name:'PBKDF2',salt:enc.encode(s),iterations:120000,hash:'SHA-256'},b,256);return Array.from(new Uint8Array(bits),x=>x.toString(16).padStart(2,'0')).join('')}async function find(table:string){const rows=(await db.list<Record<string,unknown>>(table,{limit:1})).items,id=String(rows[0]?.participantId||'');if(!id)return null;const [p]=await db.get<EduParticipant>('edu_participants',[id]);return p?{...p,id}:null}async function issue(id:string){const t=crypto.randomUUID().replace(/-/g,'')+crypto.randomUUID().replace(/-/g,''),stamp=now();await db.add(sessionTable(t),[{participantId:id,createdAt:stamp,expiresAt:new Date(Date.now()+2592000000).toISOString()}]);return t}async function participant(t:string){if(t.length<40)return null;const s=(await db.list<EduSession>(sessionTable(t),{limit:1})).items[0];if(!s||s.expiresAt<now())return null;const [p]=await db.get<EduParticipant>('edu_participants',[s.participantId]);return p?.status==='active'?{...p,id:s.participantId}:null}const effectiveRole=(p:EduParticipant):EduRole=>p.role||(p.jobRole==='Administração'?'admin':'colaborador');function pub(p:EduParticipant&{id:string}){return{id:p.id,name:p.name,phone:p.phone,email:p.email||null,jobRole:p.jobRole,role:effectiveRole(p),mustChangePassword:p.mustChangePassword,level:p.level||null,skillLevels:p.skillLevels||{},skillScores:p.skillScores||{},unitProgress:p.unitProgress||{},progress:p.progress||0,completedModules:p.completedModules||[],completedUnits:p.completedUnits||[],readingScore:p.readingScore??null,writingScore:p.writingScore??null,numeracyScore:p.numeracyScore??null,diagnosticCompletedAt:p.diagnosticCompletedAt||null}}function score(a:Record<string,string>){const skills=['leitura','compreensao','escrita','adicao','multiplicacao','divisao','porcentagem','medidas','seguranca','direitos','saude','tecnologia'];const pct=(v:string)=>{const lm=v.match(/^level:N([1-5])$/);if(lm)return (Number(lm[1])-1)*25;const parts=v.split('/'),num=Number(parts[0]),den=Number(parts[1]);return parts.length===2&&Number.isFinite(num)&&Number.isFinite(den)&&den>0?Math.max(0,Math.min(100,Math.round(num/den*100))):0};const toLevel=(x:number)=>x<12.5?'N1':x<37.5?'N2':x<62.5?'N3':x<87.5?'N4':'N5';const skillScores=Object.fromEntries(skills.map(k=>[k,pct(String(a[k]||''))]));const reviewSkills=Object.entries(skillScores).filter(([,v])=>v<50).map(([k])=>k);const skillLevels=Object.fromEntries(Object.entries(skillScores).map(([k,v])=>[k,toLevel(v)]));const readingScore=Math.round((skillScores.leitura+skillScores.compreensao)/2),writingScore=skillScores.escrita,numeracyScore=Math.round((skillScores.adicao+skillScores.multiplicacao+skillScores.divisao+skillScores.porcentagem+skillScores.medidas)/5),overallScore=Math.round(Object.values(skillScores).reduce((s,v)=>s+v,0)/skills.length),level=toLevel(overallScore);return{readingScore,numeracyScore,writingScore,overallScore,level,skillScores,skillLevels,reviewSkills}}export async function createCentralEducationSession(input:{email:string;name?:string;role?:EduRole}){const em=email(input.email);if(!em)throw new Error('E-mail corporativo inválido.');const desired=input.role&&EDU_ROLES.includes(input.role)?input.role:'colaborador';let p=await find(emailTable(em));if(!p){const s=crypto.randomUUID().replace(/-/g,''),stamp=now(),x:EduParticipant={name:input.name||em.split('@')[0],phone:'',email:em,jobRole:ADMIN_ROLES.includes(desired)?'Administração':'Colaborador',role:desired,status:'active',passwordSalt:s,passwordHash:await hash(crypto.randomUUID(),s),mustChangePassword:false,createdAt:stamp,updatedAt:stamp},[id]=await db.add('edu_participants',[x]);if(!id)throw new Error('Não foi possível preparar o acesso à Universidade.');await db.add(emailTable(em),[{participantId:id,createdAt:stamp}]);p={...x,id}}else if(input.role&&p.role!==desired){await db.update('edu_participants',[{id:p.id,record:{...p,role:desired,jobRole:ADMIN_ROLES.includes(desired)?'Administração':p.jobRole,updatedAt:now()}}]);p={...p,role:desired,jobRole:ADMIN_ROLES.includes(desired)?'Administração':p.jobRole}}return{token:await issue(p.id),participant:pub(p)}}export const EDUCATION_ROUTES={'POST /api/edu/login':[async c=>{const b=rec(c.body),id=String(b.identifier||'').trim(),ph=phone(id),em=email(id),pass=String(b.password||'');if((!ph&&!em)||pass.length<6)return error('E-mail/celular ou senha inválidos.',400);const p=await find(em?emailTable(em):phoneTable(ph));if(!p||await hash(pass,p.passwordSalt)!==p.passwordHash)return error('E-mail/celular ou senha inválidos.',401);return json({token:await issue(p.id),participant:pub(p)})}],'GET /api/edu/email/session':[requireAuth(),withScopes('email','profile'),async c=>{const em=email(c.user!.email||'');let p=await find(emailTable(em));const grant=(await db.list<Record<string,unknown>>(centralGrantTable(em),{limit:1})).items[0],validGrant=!!grant&&String(grant.email||'')===em&&String(grant.expiresAt||'')>now(),grantRole=EDU_ROLES.includes(String(grant?.role||'') as EduRole)?String(grant!.role) as EduRole:'colaborador';if(!p&&!validGrant)return error('Este e-mail não foi liberado.',403);if(!p){const s=crypto.randomUUID().replace(/-/g,''),stamp=now(),x:EduParticipant={name:c.user!.name||em.split('@')[0],phone:'',email:em,jobRole:ADMIN_ROLES.includes(grantRole)?'Administração':'Colaborador',role:grantRole,status:'active',passwordSalt:s,passwordHash:await hash(crypto.randomUUID(),s),mustChangePassword:false,createdAt:stamp,updatedAt:stamp},[id]=await db.add('edu_participants',[x]);if(!id)return error('Não foi possível preparar o acesso.',500);await db.add(emailTable(em),[{participantId:id,createdAt:stamp}]);p={...x,id}}else if(validGrant&&p.role!==grantRole){await db.update('edu_participants',[{id:p.id,record:{...p,role:grantRole,updatedAt:now()}}]);p={...p,role:grantRole}}return json({token:await issue(p.id),participant:pub(p)})}],'POST /api/edu/me':[async c=>{const p=await participant(String(rec(c.body).token||''));if(!p)return error('Sessão expirada.',401);return json({participant:pub(p)})}],'POST /api/edu/password':[async c=>{const b=rec(c.body),p=await participant(String(b.token||'')),next=String(b.password||'');if(!p)return error('Sessão expirada.',401);if(next.length<8)return error('Use ao menos 8 caracteres.',400);const s=crypto.randomUUID().replace(/-/g,'');await db.update('edu_participants',[{id:p.id,record:{...p,passwordSalt:s,passwordHash:await hash(next,s),mustChangePassword:false,updatedAt:now()}}]);return json({ok:true})}],'POST /api/edu/diagnostic':[async c=>{const b=rec(c.body),p=await participant(String(b.token||''));if(!p)return error('Sessão expirada.',401);if(p.mustChangePassword)return error('Altere a senha provisória para continuar.',403);const answers=strMap(b.answers),result=score(answers),stamp=now(),table=diagnosticTable(p.id),old=(await db.list<DiagnosticRecord>(table,{limit:1})).items[0],record:DiagnosticRecord={participantId:p.id,context:strMap(b.context),answers,...result,createdAt:stamp};if(old)await db.update(table,[{id:old.id,record}]);else await db.add(table,[record]);await db.update('edu_participants',[{id:p.id,record:{...p,...result,skillLevels:result.skillLevels,skillScores:result.skillScores,progress:Math.max(p.progress||0,10),diagnosticCompletedAt:stamp,updatedAt:stamp}}]);return json({result})}],'POST /api/edu/progress':[async c=>{const b=rec(c.body),p=await participant(String(b.token||'')),m=String(b.module||'');if(!p)return error('Sessão expirada.',401);if(p.mustChangePassword)return error('Altere a senha provisória para continuar.',403);if(!MODULES.includes(m))return error('Módulo inválido.',400);const completed=Array.from(new Set([...(p.completedModules||[]),m]));await db.update('edu_participants',[{id:p.id,record:{...p,completedModules:completed,progress:Math.round(completed.length/MODULES.length*100),updatedAt:now()}}]);return json({ok:true,completedModules:completed})}],'POST /api/edu/unit':[async c=>{const b=rec(c.body),p=await participant(String(b.token||'')),u=String(b.unit||'');if(!p)return error('Sessão expirada.',401);if(p.mustChangePassword)return error('Altere a senha provisória para continuar.',403);if(!/^(leitura|compreensao|escrita|adicao|multiplicacao|divisao|porcentagem|medidas|seguranca|direitos|saude|tecnologia)-N[1-5]$/.test(u))return error('Unidade inválida.',400);const attempts=Math.max(1,Math.min(99,Math.floor(Number(b.attempts||1)))),errors=Math.max(0,Math.min(attempts,Math.floor(Number(b.errors||0)))),hints=Math.max(0,Math.min(errors,Math.floor(Number(b.hints||errors)))),durationSec=Math.max(1,Math.min(7200,Math.floor(Number(b.durationSec||1)))),rawStats=Array.isArray(b.questionStats)?b.questionStats.slice(0,3):[],questionStats=rawStats.map(x=>{const q=rec(x);return{attempts:Math.max(0,Math.min(20,Math.floor(Number(q.attempts||0)))),errors:Math.max(0,Math.min(20,Math.floor(Number(q.errors||0))))}}),consolidation=evaluateObjectiveConsolidation(questionStats),correct=consolidation.correctItems,consolidated=consolidation.consolidated,openResponses=Array.isArray(b.openResponses)?b.openResponses.slice(0,10).map(x=>{const q=rec(x);return{question:String(q.question||'').slice(0,1000),response:String(q.response||'').slice(0,4000),itemIndex:Math.max(0,Math.floor(Number(q.itemIndex||0)))}}):[],hasOpenAnswer=b.openAnswer===true||openResponses.length>0,previous=p.unitProgress?.[u],first=!previous?.completedAt,due=!!previous&&(previous.status==='review'||previous.status==='pending_review'||!previous.nextReviewAt||Date.parse(previous.nextReviewAt)<=Date.now()),previousStage=Math.max(0,Math.min(4,Math.floor(Number(previous?.reviewStage??0)))),reviewStage=first?0:due?(errors===0?Math.min(4,previousStage+1):Math.max(0,previousStage-1)):previousStage,intervals=[1,3,7,14,30],intervalDays=intervals[reviewStage],reviewCount=Math.max(0,Math.floor(Number(previous?.reviewCount||0)))+(first?0:due?1:0),stamp=now(),nextReviewAt=new Date(Date.now()+intervalDays*86400000).toISOString(),pendingId=crypto.randomUUID(),pendingReviews=(p.pendingReviews||[]).filter(x=>x.unit!==u),pending=hasOpenAnswer?{id:pendingId,participantId:p.id,unit:u,skill:u.replace(/-N[1-5]$/,''),level:u.match(/N[1-5]$/)?.[0]||'N1',question:openResponses.map(x=>x.question).filter(Boolean).join(' | ').slice(0,1000)||'Resposta aberta da unidade',response:openResponses.map(x=>x.response).filter(Boolean).join(' | ').slice(0,4000),submittedAt:stamp,attempts,correct,errors,questionStats}:null,entry=hasOpenAnswer?{status:'pending_review',attempts,correct,errors,hints,durationSec,questionStats,reviewStage,reviewCount,intervalDays}:consolidated?{status:'consolidated',attempts,correct,errors,hints,durationSec,questionStats,reviewStage,reviewCount,intervalDays,completedAt:stamp,nextReviewAt}:{status:'practice',attempts,correct,errors,hints,durationSec,questionStats,reviewStage:previous?.reviewStage??0,reviewCount:previous?.reviewCount??0,intervalDays:previous?.intervalDays},progress={...(p.unitProgress||{}),[u]:entry},nextPendingReviews=pending?[...pendingReviews,pending]:pendingReviews,done=consolidated&&!hasOpenAnswer?Array.from(new Set([...(p.completedUnits||[]),u])):(p.completedUnits||[]).filter(x=>x!==u);await db.update('edu_participants',[{id:p.id,record:{...p,pendingReviews:nextPendingReviews,completedUnits:done,unitProgress:progress,progress:Math.round(done.length/60*100),updatedAt:stamp}}]);return json({ok:true,consolidated:consolidated&&!hasOpenAnswer,status:entry.status,pendingReview:pending,completedUnits:done,unitProgress:progress,review:consolidated&&!hasOpenAnswer?{reviewStage,reviewCount,intervalDays,nextReviewAt}:null})}],'POST /api/edu/admin/overview':[async c=>{const actor=await participant(String(rec(c.body).token||''));if(!actor)return error('Sessão expirada.',401);if(!ADMIN_ROLES.includes(effectiveRole(actor)))return error('Acesso restrito à Administração/RH.',403);const items=(await db.list<EduParticipant>('edu_participants',{limit:200})).items,participants=items.map(p=>pub({...p,id:p.id}));const pendingReviews=items.flatMap(p=>(p.pendingReviews||[]).map(r=>({...r,participantName:p.name,participantEmail:p.email||''})));return json({participants,pendingReviews,metrics:{total:participants.length,active:items.filter(p=>p.status==='active').length,diagnosed:participants.filter(p=>p.diagnosticCompletedAt).length,reinforcement:participants.filter(p=>{const s=p.skillScores||{};return Object.values(s).some(v=>v<50)}).length,publishedUnits:60,competencies:12,areas:3}})}],'POST /api/edu/admin/review':[async c=>{const b=rec(c.body),actor=await participant(String(b.token||'')),reviewId=String(b.reviewId||''),action=String(b.action||'');if(!actor)return error('Sessão expirada.',401);if(!ADMIN_ROLES.includes(effectiveRole(actor)))return error('Acesso restrito à Administração/RH.',403);if(!reviewId||!['approve','needs_revision'].includes(action))return error('Revisão inválida.',400);const items=(await db.list<EduParticipant>('edu_participants',{limit:200})).items,target=items.find(x=>(x.pendingReviews||[]).some(r=>r.id===reviewId));if(!target)return error('Resposta pendente não encontrada.',404);const pending=(target.pendingReviews||[]).find(r=>r.id===reviewId)!,remaining=(target.pendingReviews||[]).filter(r=>r.id!==reviewId),sameUnit=remaining.some(r=>r.unit===pending.unit),previous=target.unitProgress?.[pending.unit],stamp=now(),canConsolidate=action==='approve'&&!sameUnit&&evaluateObjectiveConsolidation(pending.questionStats||[]).consolidated,reviewStage=Math.max(0,Math.min(4,Math.floor(Number(previous?.reviewStage??0)))),intervalDays=[1,3,7,14,30][reviewStage],nextReviewAt=new Date(Date.now()+intervalDays*86400000).toISOString(),entry=canConsolidate?{status:'consolidated',attempts:pending.attempts,correct:pending.correct,errors:pending.errors,questionStats:pending.questionStats||[],reviewStage,reviewCount:previous?.reviewCount||0,intervalDays,completedAt:stamp,nextReviewAt}:{status:'practice',attempts:pending.attempts,correct:pending.correct,errors:pending.errors,questionStats:pending.questionStats||[],reviewStage,reviewCount:previous?.reviewCount||0,intervalDays},completedUnits=canConsolidate?Array.from(new Set([...(target.completedUnits||[]),pending.unit])):(target.completedUnits||[]).filter(x=>x!==pending.unit);await db.update('edu_participants',[{id:target.id,record:{...target,pendingReviews:remaining,completedUnits,unitProgress:{...(target.unitProgress||{}),[pending.unit]:entry},progress:Math.round(completedUnits.length/60*100),updatedAt:stamp}}]);return json({ok:true,status:entry.status,completedUnits})}],'POST /api/edu/admin/role':[async c=>{const b=rec(c.body),actor=await participant(String(b.token||''));if(!actor)return error('Sessão expirada.',401);const actorRole=effectiveRole(actor);if(!['superadmin','admin'].includes(actorRole))return error('Apenas Superadmin ou Admin pode alterar perfis.',403);const targetId=String(b.participantId||''),rawRole=String(b.role||'');if(!targetId||!EDU_ROLES.includes(rawRole as EduRole))return error('Perfil inválido.',400);const nextRole=rawRole as EduRole,[target]=await db.get<EduParticipant>('edu_participants',[targetId]);if(!target)return error('Participante não encontrado.',404);const targetRole=effectiveRole(target);if(actorRole==='admin'&&(['superadmin','admin'].includes(targetRole)||['superadmin','admin'].includes(nextRole)))return error('Admin pode gerenciar apenas RH, Gestor e Colaborador.',403);if(actorRole==='superadmin'&&targetId===actor.id&&nextRole!=='superadmin')return error('O Superadmin não pode remover o próprio acesso.',409);await db.update('edu_participants',[{id:targetId,record:{...target,role:nextRole,jobRole:ADMIN_ROLES.includes(nextRole)?'Administração':target.jobRole,updatedAt:now()}}]);return json({participant:pub({...target,id:targetId,role:nextRole,jobRole:ADMIN_ROLES.includes(nextRole)?'Administração':target.jobRole})})}]};